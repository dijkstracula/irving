#lang ivy1.8

module tcp = {
    type endpoint
    module net(msg) = {
        module socket = {
            var id: unbounded_sequence

            action send(dest: endpoint, m: msg)
            action recv(src: endpoint, m: msg)
        }
    }
}
